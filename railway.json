{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python manage.py collectstatic --no-input"
  },
  "deploy": {
    "startCommand": "python verify_production_db.py && python manage.py migrate --no-input && python create_superuser.py && python verify_deployment.py && gunicorn roomnest.wsgi:application",
    "restartPolicyType": "ON_FAILURE"
  }
}
